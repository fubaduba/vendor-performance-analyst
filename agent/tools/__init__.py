"""Tools the Vendor Performance Analyst exposes to the model.

Each tool is a plain Python function. The data layer reads from data/*.json,
which simulates the operational record that Fibey and the field agents would
have generated in the hero scenario.
"""
